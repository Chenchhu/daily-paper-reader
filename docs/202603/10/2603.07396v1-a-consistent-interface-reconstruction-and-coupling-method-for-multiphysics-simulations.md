---
title: A Consistent Interface Reconstruction and Coupling Method for Multiphysics Simulations
title_zh: 一种用于多物理场模拟的一致性界面重构与耦合方法
authors: "Ethan Huff, Savio J. Poovathingal"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07396v1"
tags: ["query:pf-roc"]
score: 8.0
evidence: 多物理场模拟的一致界面重构与耦合
tldr: 针对多物理场模拟中界面表示和通量交换的局限性，本文提出了一种一致的界面重建与耦合方法。该框架通过加权插值和步进网格技术重建连续边界，并利用保守通量映射算法实现离散单元间的数据传输。研究解决了不同离散化方式下的耦合难题，在保持几何保真度的同时确保了全局通量守恒，为离散-连续相互作用提供了通用的统一处理方案。
selection_source: fresh_fetch
motivation: 现有的多物理场耦合方法受限于底层离散化或特定物理问题，缺乏通用性且难以保证界面数据传输的一致性。
method: 提出包含加权插值步进网格重建和保守通量映射算法的两步走框架，实现连续表面重建与跨域数据投影。
result: "实验显示表面包含误差低于2.5%，通量传递误差低于1%，且瞬态模拟中的体积损失预测与解析解高度吻合。"
conclusion: 该方法建立了一个通用且可扩展的框架，为多物理场环境下的一致性界面重建和离散-连续相互作用提供了有效途径。
---

## 摘要
准确表示界面和通量交换对于广泛应用中的耦合多物理场模拟至关重要。目前，耦合方法受限于底层离散化或特定的物理问题，限制了其通用性。为了消除这些限制，本文开发了一种一致的界面重构与耦合方法，用于连接多物理计算域。所提出的数值框架包含两个互补的步骤：第一步利用加权插值和行进网格（marching-grid）方法，从离散空间数据中重构出连续的边界表面，在各种分辨率下均能保持几何保真度；第二步由保守通量映射算法组成，该算法将气动载荷、热通量或质量传递等表面量投影到附近的离散单元上，同时保持通量量的全局守恒。这种新的数值公式允许集成各种划分的区域，并确保它们之间的一致数据传输。该框架在各种几何形状上进行了测试，获得的表面包含误差低于 2.5%，通量传递误差低于 1%。均匀表面后退的瞬态模拟进一步证实了耦合演化的准确性，预测的体积损失与解析解的误差在 1% 以内。所提出的方法为多物理场环境中的一致界面重构和耦合建立了一个通用且可扩展的框架，为广泛系统的离散-连续相互作用的统一处理提供了途径。

## Abstract
Accurate representation of interfaces and flux exchange is vital for coupled multiphysics simulations across a broad range of applications. Currently, coupling approaches are limited by the underlying discretization or to specific physical problems, restricting their generality. To remove these constraints, a consistent interface reconstruction and coupling method has been developed to bridge multiphysical computational domains. The proposed numerical framework contains two complementary steps. The first step reconstructs a continuous bounding surface from discretized spatial data using a weighted interpolation and marching-grid approach that preserves geometric fidelity across a wide range of resolutions. The second step consists of a conservative flux mapping algorithm that projects surface quantities such as aerodynamic loads, heat fluxes, or mass transfer onto nearby discrete elements while maintaining global conservation of flux quantities. This new numerical formulation allows for integration of the various partitioned domains and ensures consistent data transfer between them. The framework is tested on various geometries, and surface containment errors below 2.5% and flux-transfer errors below 1% are obtained. Transient simulations of uniform surface recession further confirm accuracy in coupled evolution, with predicted volume loss agreeing with analytical solutions to within 1%. The proposed method establishes a general and extensible framework for consistent interface reconstruction and coupling in multiphysics environments, providing a pathway toward unified treatment of discrete-continuum interactions for a wide range of systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Consistent Interface Reconstruction and Coupling Method for Multiphysics Simulations》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在航空航天、能源转换和先进制造等领域，多物理场模拟（如流固耦合 FSI、材料烧蚀）至关重要。然而，现有的模拟框架面临一个核心矛盾：**离散域与连续域的表示不匹配**。
*   **背景**：固体域通常使用规则的体素（Voxel）或点集表示，缺乏明确定义的边界；而流体或连续介质求解器则需要精确的边界拟合几何体来计算表面通量。
*   **核心问题**：现有的界面处理方法（如传统的 Marching Cubes）往往会产生虚假的几何特征，且通常将表面分辨率与底层体素分辨率强制绑定，限制了物理模型的通用性和数据交换的一致性。
*   **研究动机**：开发一种能够解耦分辨率、保证几何保真度并确保通量全局守恒的通用耦合框架。

### 2. 方法论：核心思想与关键技术
论文提出了一种名为 **“步进窗口”（Marching Windows）** 的双模块框架，包含两个关键步骤：

#### A. 运动映射（Motion Mapping）—— 从体素生成表面
1.  **叠加网格**：在体素结构上叠加一层独立的笛卡尔网格（Marching Windows Grid）。
2.  **体素加权（核心创新）**：为了解决线性插值导致的表面偏移，引入了基于体素深度（$d_v$）的权重函数。边缘体素权重设为 0.5，内部体素权重随深度增加，外部“虚体素”（Ghost Voxels）权重随距离减小。
3.  **体积分数分布**：将加权后的体素体积分配到网格节点上，计算每个节点的体积分数标量场。
4.  **界面提取**：应用 Marching Squares（2D）或 Marching Cubes（3D）算法提取 50% 体积分数的等值面。

#### B. 通量映射（Flux Mapping）—— 从表面传回数据
1.  **面投影（Face Projection）**：将边缘体素的暴露面投影到附近的表面元素上，计算重叠的投影长度或面积。
2.  **保守转移**：根据投影重叠比例，将表面上的标量（如压力、热载荷、质量损失）分配给对应的体素面。该算法通过比例分配确保了**全局通量守恒**。

### 3. 实验设计
论文通过一系列几何测试和动态模拟验证了该方法的有效性：
*   **测试几何**：选择了三角形、正方形、菱形、五边形和三十边形（模拟圆形）。这些形状涵盖了锐角、与轴线成角度的边缘以及曲率特征。
*   **Benchmark（基准）**：
    *   **几何误差**：使用“质心包含误差”（Centroid Containment Error），即统计被错误包含或排除的体素/空洞面积占比。
    *   **通量误差**：应用解析的正弦通量函数，对比体素接收到的通量与解析值的偏差。
    *   **动态演化**：模拟正方形和菱形的均匀表面后退（烧蚀），并与解析体积损失曲线对比。
*   **对比变量**：研究了不同的体素比例（$v_r$）和网格比例（$g_r$）组合对精度的影响。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、内存）或具体的计算时长。
*   **算法特性**：由于该方法主要涉及几何投影和线性插值，其计算开销通常远小于底层的流体或固体物理求解器，属于轻量级耦合工具。

### 5. 实验数量与充分性
*   **实验规模**：论文对 5 种典型几何体进行了参数化研究，测试了从粗糙到精细的多种分辨率组合（$v_r$ 和 $g_r$ 从 2 到 32 的组合）。
*   **充分性评价**：实验设计较为充分。通过静态几何分析验证了空间精度，通过通量映射实验验证了物理一致性，通过瞬态烧蚀实验验证了时间演化的稳定性。
*   **客观性**：作者诚实地展示了在极低分辨率下或处理锐角时出现的“圆化效应”，并分析了误差来源，表现出较高的学术客观性。

### 6. 主要结论与发现
*   **几何精度**：在合理的网格分辨率下（$g_r \ge 8$），所有测试形状的质心包含误差均低于 **2.5%**。
*   **通量一致性**：表面到体素的平均通量转移误差低于 **1%**（对于平滑几何体）或 **2.5%**（对于复杂几何体）。
*   **动态模拟**：在表面后退模拟中，预测的体积损失与解析解的误差在 **1%** 以内，证明了该方法在耦合演化中的可靠性。
*   **分辨率解耦**：证明了表面网格分辨率可以独立于体素分辨率进行优化，这为多物理场模拟提供了极大的灵活性。

### 7. 优点与亮点
*   **通用性强**：不依赖于特定的物理求解器，可广泛应用于烧蚀、热传导、结构力学等多种耦合场景。
*   **严格守恒**：通量映射算法在数学上保证了标量在转移过程中的总量守恒，这对于能量和质量平衡至关重要。
*   **消除伪特征**：通过加权函数修正了传统 Marching Cubes 在处理体素化结构时常见的表面偏移和锯齿问题。

### 8. 不足与局限
*   **锐角圆化**：由于 Marching Squares 的线性插值特性，该方法在处理尺寸小于网格单元的极锐角特征时会产生圆化效应。
*   **3D 验证尚浅**：虽然文中给出了 3D 扩展的理论说明，但核心实验数据主要基于 2D 模拟，3D 环境下的复杂遮挡（Occlusion）处理仍需进一步研究。
*   **应用限制**：目前主要针对凸多边形或简单组合体进行了验证，对于具有极高拓扑复杂度的多孔介质内部界面的处理能力尚待观察。

（完）
